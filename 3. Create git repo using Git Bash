1. Open the Git Bash terminal.
2. In the project repository I have initialized a new Git repository by - git init
3. Create new GitHub repository by clicking on the "+" button in the top-right corner and select "New repository" and naming it as "TanyaTest"
4. Add a remote repository:
In Git Bash, run the following command to add the remote repository URL-
      git remote add origin <https://github.com/Tanya444/TanyaTest.git>
5. Add and commit your files:
      git add
6. Commit the changes with a meaningful message:
      git commit -m "Initial commit"
7. Push the repository to GitHub:
      git push -u origin master
It requires you to enter your GitHub username and password for authentication.
